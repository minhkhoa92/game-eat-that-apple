/**
 * sets directions
 * tested with strings, which does not work like integers, throws error
 */
const EnumDirection = {
  up: 0,
  right: 1,
  down: 2,
  left: 3
};

module.exports = EnumDirection;